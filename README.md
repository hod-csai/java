# jsp-webapp

Simple Maven WAR project demonstrating JSP + Servlet.

Run locally using the Tomcat Maven plugin:

```bash
mvn clean package
mvn tomcat7:run
```

Then open http://localhost:8080/ in your browser and click "Call Servlet".
